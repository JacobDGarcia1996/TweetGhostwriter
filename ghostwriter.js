function writeTweet()
{var randindex = intro = ["it has always been", "it would apear that","in our darkest hour", "A wise man once said"]
body = ["for the good of humanity","John Cena","sugar, spice, everything nice", "problem after problem", "Arceus destroyer of worlds"],
end = ["is the answer"," eror 404 entity not found"];
  var sentence = "intro [randindex]" + "body [randindex]" + "end [randindex]"
}

//Generates a random whole number between 0 and max (inclusive)
function randomUpTo()
{getElementById('randindex').innerHTML = intro ["*"];
  return Math.floor(Math.random() * (max+1));
}

function randomUpTo(){
  getElementById('randindex').innerHTML = body ["*"];
    return Math.floor(Math.random() * (max+1));
}
function randomUpTo(){
getElementById('randindex').innerHTML = end ["*"];
    return Math.floor(Math.random() * (max+1));
}
